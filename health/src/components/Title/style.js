import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

    boxTitle:{
        alignItems: 'center', // alinha os childrens (itens) dentro desse elemento ao centro
        justifyContent: 'center', // alinhar horizontal e verticalmente
        padding: 10,
    },
    textTitle:{
        color:"#FF0043",
        fontSize: 24,
        fontWeight: "bold",
    }
})

export default styles